function accum(s) {
  return s.split("-")
}
моё решение некорректное

Верное решение

function accum(s) {
  return s.split('').map((c, i) => (c.toUpperCase() + c.toLowerCase().repeat(i))).join('-');
}
